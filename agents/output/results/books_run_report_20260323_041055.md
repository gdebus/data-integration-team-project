# Run Report (20260323_041055)

- Generated at (UTC): `2026-03-23T03:10:55.219372Z`
- Run ID: `20260323_035936_books`
- Run output root: `output/runs/20260323_035936_books/`
- Validation overall (final): `0.9125`
- Validation macro (final): `0.9125`
- Sealed test overall (final): `0.8875`
- Sealed test macro (final): `0.8875`
- Correspondence structurally valid: `True`
- Final route: `finish automatic passes and export for human review`

## Agent Overview
The agent profiles the datasets, optionally reruns normalization, keeps the tested blocking setup, searches or refreshes matching when needed, generates a fusion pipeline, generates and runs an evaluation script, diagnoses the result, and then routes either back to matching, back to normalization, back to pipeline adaptation, or to final human review.

## Run Narrative
- Main problem: n/a
- Next-step advice: Protected attributes should be preserved: author=90%, language=100%, publish_year=100%, isbn_clean=100%, plus 16 attributes >85%, so no broad fusion changes should be made now. Evidence points away from blocking/matching: all 3 correspondence files are non-empty, blocking recall probes found 0 issues, and pairwise matcher F1 is decent (0.80-0.85). The biggest miss, genres=10%, is explicitly list_format_mismatch and the sample outputs show broken serialization/chunking rather than wrong entity linking. Validation expects genres as a single comma-separated string with title case; fused output contains Python-list-looking strings and split chunks, so this requires source normalization/list-column treatment before fusion, not a resolver-only change. Publisher has some true source disagreement, but also normalization problems such as 'Simon &amp; Schuster (Trade Division)' vs 'Simon & Schuster' and 'Vintage Books USA' vs 'Vintage'. Since normalization must happen first per routing rules, fix formatting/canonicalization now; only after re-evaluation should resolver/trust changes be considered. Overall headroom is small (~1.2-1.3%), so this is a ceiling-near pipeline.
- Report takeaway: The pipeline is near its structural ceiling. Blocking/matching are healthy, and most low scores come from fusion/format issues in already-matched entities. The only clearly fixable next step is normalization of list/string formatting to match the validation set, especially genres. Current fusion also uses suboptimal resolvers for publisher/title, but because pipeline_adaption cannot add preprocessing and genres failures are caused by pre-fusion format handling, normalization must be fixed first. The current inline/list handling is producing malformed nested genre strings with separator mismatches against the validation format (validation expects one plain comma-separated string, while fusion emits list-like/nested-string artifacts). Publisher also shows a format variant issue (HTML entity and imprint suffixes), which is likewise normalization-oriented before any trust-based fusion tuning.
- Top planned actions: In normalization_node, normalize genres to the validation format before fusion: parse any list-like representations safely into atomic genre items, flatten nested lists/quoted chunks, strip brackets/quotes, split on source delimiters, trim whitespace, preserve duplicates/order when present, and reserialize as one comma+space separated string matching validation title case. Do not rely on downstream union with separator='; '. Use PyDI NormalizationSpec/transform_dataframe for string cleanup and dataset-specific custom parsing where needed so each source stores genres as the same plain string format prior to matching/fusion. | In normalization_node, canonicalize publisher strings to validation style before fusion: HTML-unescape entities, strip business-unit/imprint suffixes in parentheses where validation omits them, collapse whitespace, and standardize common imprint aliases where the validation uses the shorter canonical house name. Preserve title case. After normalization and re-evaluation, consider switching resolver later to prefer_higher_trust with trust favoring metabooks_small if publisher remains underperforming. | No change in this normalization pass; preserve current title formatting because title accuracy is already relatively high and mismatches appear to be source disagreement, not a systematic format issue.

## Attempt Timeline
| attempt | recorded_at | raw_overall | accepted_overall | guard_rejected | structural_valid | invalid_pairs |
|---|---|---:|---:|---|---|---|
| 1 | 2026-03-23T03:02:39.627178Z | 0.7625 | 0.7625 | False | True |  |
| 2 | 2026-03-23T03:05:55.322201Z | 0.9125 | 0.9125 | False | True |  |
